import { BaseSystem } from "./base-system";

/**
 * Processes the message queue to be sent by gameWorld on the GameState
 * packet.
 */
export default class MessageSystem implements BaseSystem{
    constructor() {

    }

    update(dt: number): void {
        
    }
}